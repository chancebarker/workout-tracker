{
  "name": "workout-tracker-infra",
  "version": "1.0.0",
  "description": "AWS CDK (TypeScript) for the Workout Tracker serverless target (Target A).",
  "bin": { "infra": "bin/app.js" },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "diff": "cdk diff",
    "test": "echo \"Add cdk assertions tests here\" && exit 0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "aws-cdk": "2.160.0",
    "esbuild": "^0.23.0",
    "source-map-support": "^0.5.21",
    "ts-node": "^10.9.2",
    "typescript": "~5.5.4"
  },
  "dependencies": {
    "aws-cdk-lib": "2.160.0",
    "constructs": "^10.3.0"
  }
}
